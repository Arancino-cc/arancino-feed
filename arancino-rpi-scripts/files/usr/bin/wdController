#!/bin/ash

# wdController v1.4

ubus call system watchdog '{"magicclose": true}'
ubus call system watchdog '{"stop": true}'

while lsblk -f --output NAME,TYPE,STATE | grep disk > /dev/null; do

   # date 1>&2
   echo -n "X"
   sleep 3

done > /dev/watchdog

echo "Reboot after IO failure" 1>&2
